/**
 * Arrays são uma forma de você agrupar multiplos valores de uma forma linear em uma única variável.
 */

const valores = [7.7,8.9,6.3,9.2];
console.log(valores[0],valores[3]);
//trocando valores dos índices de um array
 valores[4] = 10;
 console.log(valores.length); 
 // o comando.length mostra a quantidade de arrays das sentenças de código 
//antecessoras.
//O comando .push  serve para adicionar novos elementos nos arrays ( [ elementos dentro do array]).
valores.push ("gustavo" , 12,13,14,15,16);
console.log(valores);
console.log(valores.length);
