console.log(7/0); // Qualquer número divido por zero não gera erro , gera no console a palavra (infinity).
console.log(" 10.10"/2); // No JavaScript por ser uma linguágem com tipagem fraca determinadas strings
                        // que tem valores Numéricos como esta da linha 2 são identificadas como números...
                        //  Bom ainda não sei a utilidade disso '-'
console.log("show" *2); // Em algumas linguagens esse comando mostra a string na quantidade deseja na 
                        // operação de multiplicação.
                         //Ja em JS isso não é possível o resultado no console é NaN.
console.log(0.1 + 0.7);     //  <<<<  Ainda n entendi.
console.log(Number.toString)

let a = 10;
let b = 15
let c = a+b
console.log(c.toString(2)) // Exibe o valor da variável em binário. (e  string aparentemente não sei ainda.)
